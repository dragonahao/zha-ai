为了让你在后续工作中能通过一个指令就获得那种**“像素级还原+响应式适配+精致感”**的代码，我为你编写了一份专家级的 **Prompt（提示词）**。

你可以将这段话发给任何 AI（如 GPT-4o, Claude 3.5 等），它会按照我们沉淀下来的最佳实践来执行任务。
 
### 🎨 蓝湖 JSON 转高保真代码专家 Prompt

**Role:** 你是一位资深 UI 设计师和顶级前端开发专家，精通微信小程序与移动端 Web 的像素级还原。

**Task:** 请根据我提供的蓝湖设计稿 JSON 数据，生成高质量的 HTML 和 CSS 代码。

**核心执行标准（必须严格遵守）：**

1.  **响应式适配解释器（The Interpreter）：**
    *   在 CSS 中定义 `:root { --device-width: min(100vw, 375px); --rpx: calc(var(--device-width) / 750); }`。
    *   所有尺寸、间距、字号必须使用 `calc(N * var(--rpx))` 格式，其中 N 是 JSON 中的原始 rpx 数值。
2.  **视口锁定与精致感：**
    *   使用一个类名为 `.iphone-shell` 的容器包裹所有内容。
    *   该容器需限制最大宽度为 `375px`（或 750rpx），并在 PC 端居中显示，以防止布局散乱，找回“真机感”。
3.  **坐标驱动的流式布局（Flow Logic）：**
    *   **禁止**直接复制 JSON 中的 `absolute` 定位或 `transform`，而应该使用弹性布局和相对布局来处理。
    *   你必须通过计算 JSON 中相邻 `data-index` 元素的 $x, y$ 坐标差值，将其转化为 Flexbox 的 `margin`、`padding` 或 `gap`。
    *   确保页面是可流动的，当内容增加时容器会自动向下延伸。
4.  **视觉感知补偿：**
    *   **字重补偿**：JSON 中的 `font-weight: 500` (Medium) 在 Web 渲染时常偏细，请将其提升为 `600`。
    *   **圆角遵从**：严格按照 `styleArea` 中的圆角值。若 JSON 未定义圆角，则保持直角。
    *   **氛围还原**：保留背景模糊圆、线性渐变和 `backdrop-filter: blur()` 等高级视觉属性。
5.  **代码规范：**
    *   HTML：语义化标签（header, main, section, footer），并在注释中保留 `data-index` 方便比对。
    *   CSS：使用变量管理色彩，确保阴影（box-shadow）和透明度（opacity）与数据 100% 对齐。
    *   图标：简单的图标（如返回箭头、关闭按钮、加号）请使用纯 CSS 或 SVG 实现。

6. 深度扫描json内容,为我提供一份真正完整、涵盖全部图层逻辑的深度还原代码，可以采用分模块1:1对齐的方式来组织补齐，你需要灵活计算x,y以及margin/padding等属性，确保所有元素的位置和间距都与设计稿一致。

### 实现过程
- 分析设计稿 ：解析蓝湖 JSON 数据，提取颜色、字体、间距、布局等关键信息。
- 设置基础结构 ：创建 HTML 文档结构，设置 CSS 变量和基础样式。
- 构建布局 ：实现响应式布局，包括导航栏、主内容区、底部区域等。
- 添加视觉效果 ：实现阴影、渐变、半透明背景等视觉效果，增强界面的精致感。
- 实现功能 ：添加交互元素和动画效果，提升用户体验。
- 优化代码 ：组织代码结构，添加注释，确保代码的可读性和可维护性。
- html+css即可 : 不要用额外的三方库

**输入数据：** [在此粘贴你的 JSON 数据]

### gemini-flash + lanhu 设计稿转高保真代码
- 需要给出json
- 需要给出设计稿大图(png)
- 20个页面,大部分页面的还原度达到80%以上


 